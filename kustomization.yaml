---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# The base manifests live in the base/ directory.
# This root kustomization.yaml lets you apply the whole project from the
# repository root:
#   kubectl apply -k . -n zigbee2mqtt
resources:
  - base/
